import 'package:flutter/material.dart';

class DayUpdatesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Day-to-Day Updates")),
      body: const Center(
        child: Text("This is the Day-to-Day Updates Page"),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class OtherFunctionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Other Functions")),
      body: const Center(
        child: Text("This is the Other Functions Page"),
      ),
    );
  }
}
